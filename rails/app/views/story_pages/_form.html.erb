<%= form_for(@story_page) do |f| %>
  <% if @story_page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@story_page.errors.count, "error") %> prohibited this story_page from being saved:</h2>

      <ul>
      <% @story_page.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
 <div class="field">
    <%= f.label :page_number %><br>
    <%= f.number_field :page_number %>
 
  </div>

  <div class="field">
    <%= f.label :storytype %><br>
    <% f.text_field :storytype %>
    <%= select("story_page", "storytype", ["VoicePageViewController", "DrawingPrompterViewController", "DrawingPageViewController", "UnscramblePageViewController", "BasePageViewController"].map{|p| [ p, p ] })%>
  </div>
  <div class="field">
    <%= f.label :story_id %><br>
     <%= select("story_page", "story_id", Story.all.collect {|p| [ p.title, p.id ] }) %>
   </div>
   <br>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
